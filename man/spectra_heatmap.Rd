% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{spectra_heatmap}
\alias{spectra_heatmap}
\title{Plot Spectra Heatmap}
\usage{
spectra_heatmap(
  obj,
  ...,
  foc_rgn = NULL,
  what = c("si", "sup", "supal"),
  cols = NULL,
  tol = 0.001,
  tp_col = "black",
  corr_col = "green",
  wrong_col = "red",
  miss_col = "yellow",
  border_lwd = 2,
  xlab = "Chemical Shift [ppm]",
  ylab = "Spectrum",
  mar = c(4.1, 4.1, 1.1, 0.1),
  lgd = list()
)
}
\arguments{
\item{obj}{An object of type \code{spectrum}, \code{spectra}, \code{decon2}, \code{decons2}, \code{align} or
\code{aligns}. For details see \link{metabodecon-classes}.}

\item{...}{Additional arguments passed to \code{\link[=as_spectra]{as_spectra()}}.}

\item{foc_rgn}{Numeric vector of length 2 specifying the focus region in ppm
(e.g. \code{c(3.55, 3.35)}). If NULL (default), the full spectrum is shown.}

\item{what}{Which signal to plot: \code{"si"} (raw, default), \code{"sup"} (superposition of
Lorentz curves) or \code{"supal"} (aligned superposition).}

\item{cols}{Character vector of colors used as intensity color palette. Defaults to
\code{hcl.colors(64, "YlOrRd", rev = TRUE)}.}

\item{tol}{Numeric tolerance in ppm used to match deconvoluted peaks to true peaks.}

\item{tp_col, corr_col, wrong_col, miss_col}{Border colors for true (un-deconvoluted), correctly identified, wrongly
identified and missed peaks.}

\item{border_lwd}{Line width used for the peak border rectangles.}

\item{xlab, ylab}{Axis labels.}

\item{mar}{Numeric vector of length 4 specifying the plot margins. Passed to \code{\link[=par]{par()}}.}

\item{lgd}{Logical or list. If TRUE, a legend is drawn at "topright" with \code{cex = 0.8}.
If a list, its elements are passed to \code{\link[=legend]{legend()}} to override position, size,
etc. Set \code{show = FALSE} (or pass \code{lgd = FALSE}) to hide.}
}
\value{
NULL. Called for side effect of plotting.
}
\description{
Plot a set of spectra as a heatmap. Each row corresponds to one spectrum,
each column to a chemical-shift datapoint, and the signal intensity is
color-coded.

If the spectra were simulated (i.e. carry a \code{simpar} element in
\code{meta}), the true peaks are highlighted with thick rectangles spanning
\code{x0 +- lambda}. If the spectra have additionally been deconvoluted, the
rectangles are colored according to whether each peak was correctly
identified (green), missed (yellow) or wrongly identified (red, drawn at
the position of the deconvoluted peak).
}
\examples{
obj <- deconvolute(sim[1:4], sfr = c(3.55, 3.35))
spectra_heatmap(obj)
spectra_heatmap(obj, foc_rgn = c(3.55, 3.35))
}
\author{
2024-2026 Tobias Schmidt: initial version.
}
