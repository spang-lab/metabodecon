% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{sap2}
\alias{sap2}
\title{The Sap2 Mini Classification Dataset}
\format{
A \code{spectra} object consisting of 18 \code{spectrum} objects, where each
spectrum contains 128 datapoints ranging from 3.59 to 3.44 ppm. The
per-spectrum group labels are attached as \code{attr(sap2, "group")}, a named
factor with levels \code{A} and \code{B}. For details about \code{spectrum} and
\code{spectra} objects see \link{metabodecon-classes}.

Each spectrum's \code{meta$simpar} carries the standard fields (\code{x0}, \code{A},
\code{lambda}, \code{noise}) plus four sap2-specific fields: \code{base_x0} (the 4
reference peak positions, identical across spectra), \code{dx0} (per-peak
jitter in ppm), \code{gx0} (scalar global ppm shift), and \code{diff_AB} (integer
index into \code{base_x0} of the peak that differs between groups). They
satisfy \code{x0[k] = base_x0[k] + dx0[k] + gx0}.

\code{attr(sap2, "true_x0")} is a numeric vector with the post-alignment ppm
positions of the discriminating peak.
}
\usage{
sap2
}
\description{
A small simulated two-group classification dataset, designed as a
didactic counterpart to \link{sim2}. It contains 18 simulated 1D
NMR spectra (128 datapoints each) split evenly into groups \code{A} and \code{B},
where 1 out of 4 peaks per spectrum differs between groups by 10\% in
area. The dataset is small enough that the full feature matrix produced
by \code{\link[=si_mat]{si_mat()}} can be displayed in its entirety, which makes
it useful for visualizing the deconvolution / alignment / feature
extraction pipeline end-to-end.
}
\details{
Peak shape sampling matches \link{sim2} at ~6-9 datapoints per
half-width, so deconvolution behaves analogously to sim2. The
ppm/datapoint resolution is coarser (\verb{0.0012 ppm/dp} vs. \verb{0.00015 ppm/dp} in sim2) so that 4 well-separated peaks fit into 128
datapoints. Concretely:
\itemize{
\item 4 base peaks per spectrum at positions \code{c(3.555, 3.525, 3.495, 3.465)}
ppm, leaving signal-free regions of ~29 dp on the left and ~22 dp on
the right edge for noise estimation.
\item Per-peak jitter (sd \code{0.0012} ppm, ~1 dp) plus a per-spectrum global
ppm shift (sd \code{0.0048} ppm, ~4 dp) so alignment is visibly necessary.
\item Base areas drawn from a log-normal distribution centered around \code{2500}
(in ppm-area units) and varied per spectrum by \verb{+/-60\%}.
\item Base half-widths drawn uniformly in \verb{[0.0072, 0.0108]} ppm and varied
per spectrum by \verb{+/-10\%}.
\item Gaussian noise with standard deviation \code{1500}.
\item In group \code{A}, the second base peak has its area multiplied by \code{1.1}.
The exact index is stored in \code{simpar$diff_AB}.
}
}
\keyword{datasets}
