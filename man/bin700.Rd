% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simat.R
\name{bin700}
\alias{bin700}
\title{700-bin Zacharias 2013 feature matrix}
\usage{
bin700(x, maxCombine = 0L, igrs = list(), peakPos = NULL, ...)
}
\arguments{
\item{x}{A \code{spectra}, \code{decons2}, or \code{aligns} object.}

\item{maxCombine}{Ignored. Accepted for protocol compatibility.}

\item{igrs}{Ignored. Accepted for protocol compatibility.}

\item{peakPos}{Ignored. Accepted for protocol compatibility.}

\item{...}{Ignored.}
}
\value{
A numeric matrix with one row per spectrum and 700 columns
of bin sums. Column names are \code{sprintf("\%.4f", bin_midpoint)};
row names are spectrum names.
}
\description{
Builds a feature matrix on the fixed 700-bin grid of Zacharias
(2013): 300 bins covering 6.5-9.5 ppm + 400 bins covering 0.5-4.5
ppm, both at 0.01 ppm width. The water region (4.5-6.5 ppm) is
excluded. Bins are ordered high-to-low — column 1 covers
(9.49, 9.50) ppm, column 700 covers (0.50, 0.51) ppm.

Suitable as the \code{feat_fun} argument of \code{\link[=fit_mdm]{fit_mdm()}}.
Per-spectrum dispatch:
\itemize{
\item If \code{lcpar} is empty (raw spectra): bin \verb{$si} directly.
\item If \code{lcpar} is non-empty (deconvoluted / aligned spectra):
reconstruct \code{si_hat = lorentz_sup(cs, lcpar)} on the spectrum's
own \code{cs} grid using \code{x0al} when present (else \code{x0}), then bin
the reconstruction.
}

Always returns all 700 columns; \code{maxCombine}, \code{igrs} and \code{peakPos}
are accepted for \code{feat_fun} protocol compatibility but ignored
(the bin layout is hardcoded).
}
\author{
2026 Tobias Schmidt: initial version.
}
