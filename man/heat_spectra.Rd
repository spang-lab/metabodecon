% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{heat_spectra}
\alias{heat_spectra}
\title{Plot Spectra Heatmap}
\usage{
heat_spectra(
  objs,
  foc_rgn = NULL,
  what = NULL,
  cols = NULL,
  xlab = "Chemical Shift [ppm]",
  ylab = "Spectrum",
  mar = c(4.1, 2.1, 1.1, 0.5),
  y = NULL,
  y_cols = NULL,
  true_x0 = NULL,
  true_col = "darkgreen",
  true_tol = NULL,
  scale_cols = FALSE,
  cex_names = 0.8,
  xaxis_side = 1,
  col_scores = NULL,
  col_sep = NULL,
  row_sep = NULL,
  main = NULL,
  names = NULL,
  ref = NULL,
  ref_col = "red",
  ref_lwd = NULL,
  sparse = FALSE,
  xaxt = "s"
)
}
\arguments{
\item{objs}{An object of type \code{spectrum}, \code{spectra}, \code{decon2}, \code{decons2}, \code{align} or
\code{aligns}, OR a numeric matrix with chemical-shift values as \code{colnames}
(rows are spectra). To plot a feature matrix derived from peak areas, use
\code{\link[=si_mat]{si_mat()}} and pass the result.}

\item{foc_rgn}{Numeric vector of length 2 specifying the focus region in ppm
(e.g. \code{c(3.55, 3.35)}). If NULL (default), the full spectrum is shown.}

\item{what}{Which signal to plot: \code{"si"} (raw), \code{"sup"} (superposition of Lorentz
curves) or \code{"supal"} (aligned superposition). Defaults to a sensible
choice based on the input class. Ignored when \code{objs} is a matrix.}

\item{cols}{Character vector of colors used as intensity color palette. Defaults to
\code{hcl.colors(64, "YlOrRd", rev = TRUE)}.}

\item{xlab, ylab}{Axis labels.}

\item{mar}{Numeric vector of length 4 specifying the plot margins. Passed to \code{\link[=par]{par()}}.
The right margin is overridden at runtime to fit the spectra names.}

\item{y}{Optional vector of class labels (one per spectrum). If provided, the
spectra names are colored according to the class.}

\item{y_cols}{Character vector of colors used to color the spectra names by class.
Defaults to \code{rainbow(nlevels(as.factor(y)))}. Ignored if \code{y} is \code{NULL}.}

\item{true_x0}{Optional numeric vector of true peak positions (in ppm). If supplied, the
x-axis tick labels of columns within \code{true_tol} ppm of any \code{true_x0} are
drawn in \code{true_col}. Useful to highlight known discriminating features in
a sparse feature matrix from \code{\link[=si_mat]{si_mat()}}.}

\item{true_col}{Color used for x-axis labels of columns close to a \code{true_x0} value.}

\item{true_tol}{Numeric tolerance in ppm for matching columns to \code{true_x0}. Defaults to
half the median column spacing.}

\item{scale_cols}{If \code{TRUE}, scale each column (chemical shift) to a symmetric range
before mapping to colours, so per-feature contrasts are comparable.}

\item{cex_names}{Character expansion factor for the spectrum name labels drawn on the
right side of the heatmap. Defaults to \code{0.8}.}

\item{xaxis_side}{On which side to draw the x-axis: \code{1} (bottom, default) or \code{3} (top).}

\item{col_scores}{Optional numeric vector of length \code{ncol(Z)} (after \code{foc_rgn} filtering)
giving a per-column score (e.g. lasso coefficients or feature
importances). When supplied, columns are sorted by \code{col_scores} (ascending)
and the score is appended in brackets to each x-axis label.}

\item{col_sep}{Vertical column separators. \code{NULL} (default) draws a separator at the
sign change of \code{col_scores} if given, otherwise none. \code{FALSE} disables
separators entirely. An integer vector draws separators \emph{after} the given
(post-sort) column indices.}

\item{row_sep}{Horizontal row separators. \code{NULL} (default) draws separators at class
changes when \code{y} is given, otherwise none. \code{FALSE} disables separators
entirely. An integer vector draws separators \emph{after} the given row
indices.}

\item{main}{Optional plot title. Drawn via \code{\link[graphics:title]{graphics::title()}}.}

\item{names}{Controls per-spectrum names drawn on the right side. \code{NULL} (default)
or \code{TRUE} uses the spectrum names. \code{FALSE} hides them and shrinks the
right margin accordingly. A character vector overrides the labels.}

\item{ref}{Integer row index (1-based) of a reference spectrum to highlight with
a rectangle, or \code{NULL} (default) for no highlight.}

\item{ref_col}{Color of the reference-row rectangle. Defaults to \code{"red"}.}

\item{ref_lwd}{Line width of the reference-row rectangle. Defaults to \code{par("lwd")}.}

\item{sparse}{If \code{TRUE}, render the heatmap as a sparse peak matrix: all cells are
zero except at the columns where a peak center sits (picked from
\code{lcpar$pcisn} / \code{lcpar$pcial} / \code{lcpar$pcide}, in that priority).
The value at a peak column is the Lorentzian peak height \code{A / lambda};
collisions on the same column are summed. Ignored when \code{objs} is a
matrix.}

\item{xaxt}{Character. \code{"s"} (default) draws the x-axis normally; \code{"n"}
suppresses x-axis ticks, tick labels and \code{xlab}.}
}
\value{
NULL. Called for side effect of plotting.
}
\description{
Plot a set of spectra as a heatmap. Each row corresponds to one spectrum,
each column to a chemical-shift datapoint, and the signal intensity is
color-coded.

If the spectra were simulated (i.e. carry a \code{simpar} element in
\code{meta}), the true peaks are highlighted with thick rectangles spanning
\code{x0 +- lambda}. If the spectra have additionally been deconvoluted, the
rectangles are colored according to whether each peak was correctly
identified (green), missed (yellow) or wrongly identified (red, drawn at
the position of the deconvoluted peak).
}
\examples{
obj <- deconvolute(sim[1:4], sfr = c(3.55, 3.35))
heat_spectra(obj)
heat_spectra(obj, foc_rgn = c(3.55, 3.35))
}
\author{
2024-2026 Tobias Schmidt: initial version.
}
