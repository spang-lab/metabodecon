% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wip.R
\name{estimate_svm_performance}
\alias{estimate_svm_performance}
\title{Estimate SVM Performance}
\usage{
estimate_svm_performance(
  X,
  y,
  test_ids,
  costs,
  gammas,
  nfeats,
  inner_nfolds = 20,
  verbose = TRUE,
  ncores = 8L,
  sample_ids = NULL
)
}
\arguments{
\item{X}{Feature matrix with samples in rows.}

\item{y}{Binary labels (factor or 0/1).}

\item{test_ids}{List of test index vectors for outer folds.}

\item{costs}{Cost parameter candidates for grid search.}

\item{gammas}{Gamma parameter candidates for grid search.}

\item{nfeats}{Feature count candidates for grid search.}

\item{inner_nfolds}{Number of inner CV folds for hyperparameter tuning.}

\item{verbose}{Print progress messages.}

\item{ncores}{Number of CPU cores for parallel grid search.}

\item{sample_ids}{Optional sample IDs. Defaults to row indices.}
}
\value{
Data frame with columns \code{sample}, \code{fold}, \code{cost}, \code{gamma},
\code{nfeat}, \code{true}, \code{prob}, \code{pred}.
}
\description{
Evaluates radial SVM using nested cross-validation.
}
\examples{
\dontrun{
X <- matrix(rnorm(200), 20)
y <- factor(rep(c("Control", "AKI"), each = 10))
te <- list(1:5, 6:10, 11:15, 16:20)
Y <- estimate_svm_performance(X, y, te,
  costs = 2^(0:2), gammas = 2^(-5:-3), nfeats = 2:3)
head(Y)
}
}
