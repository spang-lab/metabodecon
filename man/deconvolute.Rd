% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decon.R
\name{deconvolute}
\alias{deconvolute}
\alias{get_deg}
\title{Deconvolute one or more NMR spectra}
\usage{
deconvolute(
  x,
  nfit = 3,
  smit = 2,
  smws = 5,
  delta = 6.4,
  npmax = 0,
  sfr = NULL,
  igrs = list(),
  use_rust = FALSE,
  verbose = TRUE,
  nworkers = 1
)

get_deg(conf = "default")
}
\arguments{
\item{x}{A \code{spectrum} or \code{spectra} object as described in \link{metabodecon-classes}.}

\item{nfit}{Integer. Number of iterations for approximating the parameters
for the Lorentz curves. See 'Details'.}

\item{smit}{Integer. Number of smoothing iterations. See 'Details'.}

\item{smws}{Integer. Smoothing window size (number of data points; must be
odd). See 'Details'.}

\item{delta}{Threshold for peak filtering. Higher values result in more peaks
being filtered out. A peak is filtered if its score is below \eqn{\mu +
\sigma \cdot \delta}{mu + s * delta}, where \eqn{\mu}{mu} is the average
peak score in the signal-free region (SFR), and \eqn{\sigma}{s} is the
standard deviation of peak scores in the SFR. See 'Details'.}

\item{npmax}{Integer scalar in \verb{\{-2, -1, 0, 1, 2, ...\}} controlling
how \verb{(nfit, smit, smws, delta)} are chosen. If \code{npmax >= 1}, those four
arguments are ignored and a grid search over predefined parameter
combinations is performed instead — the combination with the smallest
residual area ratio and fewer than \code{npmax} peaks is selected. Grid
search results are cached to disk automatically. \code{npmax = 0} (default)
disables the grid search and uses the literal \verb{(nfit, smit, smws, delta)} arguments. \code{npmax = -1} is "auto": resolved up front to a
single integer (the median per-spectrum Kneedle elbow on \verb{$deg}) and
broadcast to every spectrum. \code{npmax = -2} is "intrinsic": resolved
per spectrum to that spectrum's own Kneedle elbow, so different
spectra get different \code{npmax} values.}

\item{sfr}{Numeric vector with two entries: the ppm positions for the left
and right border of the signal-free region of the spectrum. See 'Details'.}

\item{igrs}{Ignore regions. List of length-2 numeric vectors specifying the
start and endpoints of the chemical shift regions to ignore during
deconvolution. Peaks whose centers fall inside any ignore region are
excluded from fitting.}

\item{use_rust}{Controls the deconvolution backend. \code{FALSE} or any numeric
value \verb{< 1} (default) uses the R implementation. \code{TRUE} or any numeric
value \verb{>= 1} uses the Rust backend via
\href{https://github.com/spang-lab/mdrb}{mdrb}. \code{NULL} auto-detects: uses Rust
if available, otherwise R. When set to \code{TRUE} / \verb{>= 1} and mdrb is not
installed, an error is thrown.}

\item{verbose}{Logical. Whether to print log messages during the
deconvolution process.}

\item{nworkers}{Number of workers to use for parallel processing. If
\code{"auto"}, the number of workers will be determined automatically. If a number
greater than 1, it will be limited to the number of spectra.}

\item{conf}{Character string selecting a configuration. Currently only
\code{"default"} is supported.}
}
\value{
A 'decon2' object as described in \link{metabodecon-classes}.

A data frame with columns \code{nfit}, \code{smit}, \code{smws}, \code{delta}.
}
\description{
Deconvolutes NMR spectra by modeling each detected signal within
a spectrum as Lorentz Curve.

Returns the default grid of \verb{(nfit, smit, smws, delta)} combinations used
by \code{\link[=deconvolute]{deconvolute()}} when \code{npmax >= 1}. Useful as the \code{deg}
argument to \code{\link[=fit_mdm]{fit_mdm()}}.
}
\details{
First, an automated curvature based signal selection is performed. Each
signal is represented by 3 data points to allow the determination of initial
Lorentz curves. These Lorentz curves are then iteratively adjusted to
optimally approximate the measured spectrum.
}
\examples{
## Deconvolute a single spectrum
spectrum <- sim[[1]]
decon <- deconvolute(spectrum)

## Read multiple spectra from disk and deconvolute at once
spectra_dir <- metabodecon_file("sim_subset")
spectra <- read_spectra(spectra_dir)
decons <- deconvolute(spectra, sfr = c(3.55,3.35))
get_deg()
}
\author{
2024-2025 Tobias Schmidt: initial version.
}
