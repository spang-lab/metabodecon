% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mdlm.R
\name{mdlm}
\alias{mdlm}
\alias{benchmark_mdlm}
\alias{fit_mdlm}
\alias{cv_mdlm}
\alias{mdlm_grid}
\title{Metabodecon Deconvolved Linear Models}
\usage{
benchmark_mdlm(
  spectra,
  y,
  sfr,
  nwo = 1,
  nwi = 1,
  verbose = TRUE,
  use_rust = TRUE,
  grid = mdlm_grid(),
  nfo = 5,
  nfi = 10,
  seed = 1
)

fit_mdlm(
  spectra,
  y,
  sfr,
  use_rust = TRUE,
  nfit = 5,
  smit = 2,
  smws = 5,
  delta = 4,
  maxShift = 100,
  maxSnap = 20,
  nworkers = 1,
  verbose = TRUE,
  nfolds = 10,
  foldid = NULL
)

cv_mdlm(
  spectra,
  y,
  sfr,
  use_rust = TRUE,
  grid = mdlm_grid(),
  nworkers = 1,
  verbose = TRUE,
  nfolds = 10,
  seed = 1
)

mdlm_grid(
  nfit = 5,
  smit = 2,
  smws = c(5, 7),
  delta = c(4, 6, 8),
  maxShift = c(50, 100, 200, 300),
  maxSnap = c(10, 20, 30, 40)
)
}
\arguments{
\item{spectra}{List-like spectra object with \code{cs} and \code{si} vectors.}

\item{y}{Factor vector with class labels for each spectrum.}

\item{sfr}{Signal free region. See \code{\link[=deconvolute]{deconvolute()}} for details.}

\item{nwo}{Number of workers for the outer cross-validation.}

\item{nwi}{Number of workers for the inner cross-validation.}

\item{verbose}{Logical. Whether to print log messages.}

\item{use_rust}{Logical. Whether to use the Rust backend.}

\item{grid}{Data frame of preprocessing parameter combinations as returned by
\code{\link[=mdlm_grid]{mdlm_grid()}}. It must contain the columns \code{nfit}, \code{smit},
\code{smws}, \code{delta}, \code{maxShift} and \code{maxSnap}.}

\item{nfo, nfi}{Number of outer/inner folds.}

\item{seed}{Random seed for fold assignment.}

\item{nfit}{Fitting iterations to try in the grid search.}

\item{smit}{Smoothing iterations to try in the grid search.}

\item{smws}{Smoothing window sizes to try in the grid search.}

\item{delta}{Peak-filter thresholds to try in the grid search.}

\item{maxShift}{Maximum alignment shifts to try in the grid search.}

\item{maxSnap}{Maximum snap distances to try in the grid search.}

\item{nworkers}{Number of workers for parallel grid evaluation.}

\item{nfolds}{Number of folds for inner \code{cv.glmnet()}. Default 10.}

\item{foldid}{Optional integer vector of fold ids passed to
\code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}}. If provided, \code{nfolds} is ignored.}
}
\value{
\code{\link[=mdlm_grid]{mdlm_grid()}} returns a data frame with one row per
preprocessing combination.

\code{\link[=fit_mdlm]{fit_mdlm()}} returns an object of class \code{mdlm} with elements
\code{model}, \code{ref} and \code{meta}.

\code{\link[=cv_mdlm]{cv_mdlm()}} returns an \code{mdlm} object with additional elements
\code{pgrid} and \code{ibest}.

\code{\link[=benchmark_mdlm]{benchmark_mdlm()}} returns a list with elements:
\itemize{
\item \code{models}: List of \code{mdlm} objects, one per fold.
\item \code{predictions}: Data frame with columns \code{fold}, \code{true}, \code{prob}, \code{pred}.
}
}
\description{
Utilities for fitting, tuning and benchmarking metabodecon lasso models after
deconvolution, alignment and peak snapping.

\code{\link[=mdlm_grid]{mdlm_grid()}} builds the preprocessing grid. By default, the
grid varies over \code{smws}, \code{delta}, \code{maxShift} and \code{maxSnap}, while keeping
\code{nfit = 5} and \code{smit = 2} fixed. Passing longer vectors for \code{nfit} or \code{smit}
expands the grid accordingly.

\code{\link[=fit_mdlm]{fit_mdlm()}} fits one cross validated lasso model for a single
preprocessing setting. Its in-process cache reuses deconvolutions for equal
\code{nfit}/\code{smit}/\code{smws}/\code{delta} and reuses alignments for equal
\code{nfit}/\code{smit}/\code{smws}/\code{delta}/\code{maxShift}.

\code{\link[=cv_mdlm]{cv_mdlm()}} evaluates the full preprocessing grid and selects
the parameter combination that produced the lowest mean cross-validated
error. It returns the final model

\code{\link[=benchmark_mdlm]{benchmark_mdlm()}} wraps \code{\link[=cv_mdlm]{cv_mdlm()}} into an outer
cross-validation loop for performance estimation. It returns a list with the
per-fold models (obtained via \code{\link[=cv_mdlm]{cv_mdlm()}}) and the per-fold
predictions from the held-out test sets.
}
\details{
Worker counts in \code{\link[=cv_mdlm]{cv_mdlm()}} are best chosen relative to cache
boundaries in the ordered grid. With the default grid there are 96 rows,
grouped into 6 deconvolution blocks (\code{nfit}/\code{smit}/\code{smws}/\code{delta}) of 16 rows
each, and each such block contains 4 alignment blocks
(\code{nfit}/\code{smit}/\code{smws}/\code{delta}/\code{maxShift}) of 4 rows each. Changing the grid
changes these block sizes, so the worker counts that best preserve
cache-sharing boundaries change as well.

Therefore:
\itemize{
\item \verb{nworkers = 1, 2, 3, 6} preserves both deconvolution and alignment cache
boundaries.
\item \verb{nworkers = 4, 8, 12, 24} preserves only alignment-cache boundaries.
\item Other worker counts are valid but split more cache-sharing groups across
workers, potentially increasing total runtime and memory use.
}

Each outer worker in \code{\link[=benchmark_mdlm]{benchmark_mdlm()}} keeps its own copy of
\code{spectra} and \code{y}, so memory use grows roughly with \code{nwo}, not with \code{nfo}.
Large \code{nwo} values can therefore still require substantial RAM for large
input datasets.
}
\examples{
\dontrun{
     g <- mdlm_grid()
     m <- fit_mdlm(spectra, y, sfr = c(11, -2))
     cv <- cv_mdlm(spectra, y, sfr = c(11, -2), grid = g)
     bm <- benchmark_mdlm(spectra, y, sfr = c(11, -2), grid = g)
}
}
