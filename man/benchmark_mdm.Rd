% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mdm.R
\name{benchmark_mdm}
\alias{benchmark_mdm}
\title{Estimate MDM Performance via Nested Cross-Validation}
\usage{
benchmark_mdm(
  spectra,
  y,
  sfr,
  nwo = 1,
  nwi = 1,
  verbose = TRUE,
  use_rust = TRUE,
  nfo = 5,
  nfi = 10,
  seed = 1,
  cadir = decon_cachedir()
)
}
\arguments{
\item{spectra}{List-like spectra object with \code{cs} and \code{si} vectors.}

\item{y}{Factor vector with class labels for each spectrum.}

\item{sfr}{Signal free region. See \code{\link[=deconvolute]{deconvolute()}} for details.}

\item{nwo}{Number of workers for the outer cross-validation. Each worker gets
a full copy of \code{spectra} and \code{y}.}

\item{nwi}{Number of workers used inside each \code{cv_mdm()} fit and prediction.}

\item{verbose}{Logical. Whether to print log messages.}

\item{use_rust}{Logical. Whether to use the Rust backend.}

\item{nfo}{Number of outer folds. Default 3.}

\item{nfi}{Number of folds for inner cv.glmnet. Default 3.}

\item{seed}{Random seed for fold assignment.}

\item{cadir}{Directory for caching deconvolution grid search results.
Defaults to \code{\link[=decon_cachedir]{decon_cachedir()}}. If \code{NULL}, caching is disabled.
Pass a custom path to use a different cache directory.}
}
\value{
A list with elements:
\itemize{
\item \code{models}: List of objects with classes \code{cv} and \code{mdm}, one per fold.
\item \code{predictions}: Data frame with columns \code{fold}, \code{true}, \code{prob}, \code{pred}.
}
}
\description{
Splits the data into \code{nfo} outer folds. For each fold, calls
\code{\link[=cv_mdm]{cv_mdm()}} on the training subset to select the best preprocessing
parameters and fit a model, then evaluates on the held-out test fold.
Grid search results are shared across all folds via a temporary cache
directory.
}
\details{
Each outer worker keeps its own copy of \code{spectra} and \code{y}, so memory use
grows roughly with \code{nwo}, not with \code{nfo}. Large \code{nwo} values can therefore
still require substantial RAM for large input datasets.
}
\examples{
\dontrun{
     aki <- read_aki_data()
     spectra <- aki$spectra
     y <- factor(aki$meta$type, levels = c("Control", "AKI"))
     stub(benchmark_mdm, spectra=spectra, y=y, sfr = c(11, -2), nwo = 5, nwi = 11)
     bm <- benchmark_mdm(spectra=spectra, y=y, sfr = c(11, -2), nwo = 5, nwi = 11)
}
}
