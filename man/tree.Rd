% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util.R
\name{tree}
\alias{tree}
\alias{tree_preview}
\title{Print the Structure of a Directory Tree}
\usage{
tree(
  path,
  max.level = 2,
  max.entries = Inf,
  show.counts = FALSE,
  files.first = FALSE,
  level = 0,
  prefix = ""
)

tree_preview(
  path,
  max.level = 1,
  max.entries = 5,
  show.counts = TRUE,
  files.first = TRUE
)
}
\arguments{
\item{path}{The root path from which to start listing the directory
structure.}

\item{max.level}{The maximum depth of directories to list.}

\item{max.entries}{Maximum number of children to print per directory.
If a directory has more entries than this limit, the first
\code{ceiling(max.entries / 2)} and last \code{floor(max.entries / 2)}
children are shown, with \code{... [N]} in between, where \code{N} is the
number of omitted children.}

\item{show.counts}{Logical. If \code{TRUE}, prints the number of direct
children (files + subdirectories) in brackets after each directory name.
Disabled by default.}

\item{files.first}{Logical. If \code{TRUE}, files are listed before
subdirectories. If \code{FALSE} (default), subdirectories are listed first.}

\item{level}{Internal parameter used for recursion, indicating the current
level of depth.}

\item{prefix}{Internal parameter used for formatting the printed tree
structure.}
}
\value{
NULL, called for its side effect of printing the directory structure.
}
\description{
Prints the structure of a directory tree up to a specified maximum level of
depth. It lists all files and directories under the specified path,
displaying them in a tree-like structure.
}
\examples{
metabodecon_dir <- system.file(package = "metabodecon")
tree(metabodecon_dir, max.level = 1)
}
\author{
2024-2025 Tobias Schmidt: initial version.
}
