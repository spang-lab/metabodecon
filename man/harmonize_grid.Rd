% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{harmonize_grid}
\alias{harmonize_grid}
\title{Harmonize a corpus of spectra onto a shared chemical-shift grid}
\usage{
harmonize_grid(x, target = "median", pad = 0)
}
\arguments{
\item{x}{A \code{spectra} object (or list of \code{spectrum} objects).}

\item{target}{Either \code{"median"} (default) — anchor the target grid
at the median first-ppm across \code{x} — or a numeric vector of
length \code{length(x[[1]]$cs)} giving an explicit target grid.}

\item{pad}{Numeric scalar used to fill the vacated edge after the
shift. Default \code{0}. Use \code{NA} if you want downstream code to
detect the borrowed region explicitly.}
}
\value{
A \code{spectra} object with every spectrum's \verb{$cs} replaced by
the target grid and \verb{$si} shifted accordingly. All other fields
(\verb{$meta}, \verb{$lcpar}, \verb{$sit}, ...) are preserved unchanged.
}
\description{
Pre-aligns every spectrum in \code{x} to a single shared chemical-shift
grid by integer-datapoint shifting. After the call, every spectrum's
\verb{$cs} is bit-identical to the chosen target grid, so downstream code
(deconvolution, \code{\link[=clupa]{clupa()}}, \code{\link[=snap_to_ref]{snap_to_ref()}},
feature-matrix builders) can index everyone by datapoint and treat
\code{cs} as a single shared variable.

All input spectra must share the same point count and the same ppm
width (calibration offsets are allowed, frequency-domain resolution
differences are not). For each spectrum the integer offset from the
target grid is computed in datapoints, \verb{$si} is rolled by that
many positions, the vacated edge is filled with \code{pad} (default \code{0}),
and \verb{$cs} is replaced by the target grid.

This kills the per-spectrum absolute-calibration offset between
acquisitions (typically a constant shift in ppm coming from
spectrometer reference setup) so that downstream CluPA only has to
correct the remaining sub-datapoint residual + the random
chemical-shift drift from sample composition.

Sub-datapoint residual: at most ±0.5 datapoint per spectrum, which
is two orders of magnitude smaller than typical Lorentzian peak
widths (lambda ~ 1e-3 to 1e-2 ppm vs. spacing ~ 1e-4 ppm), so the
rounding error is irrelevant for any downstream fit.

Edge handling: a spectrum that needs to shift right by \code{k}
datapoints loses \code{k} datapoints from one end and gains \code{k} \code{pad}
values on the other. For typical NMR spectra the edges are noise,
so zero-padding is equivalent to dropping the noise — no real
signal is harmed. For large absolute shifts (tens of datapoints)
you can verify the discarded region is noise by inspecting \verb{$cs}
against the metabolite range of interest.
}
\examples{
\dontrun{
  aki <- metabodecon:::read_aki_data()
  x   <- harmonize_grid(aki$spectra)
  all(sapply(x, function(s) identical(s$cs, x[[1]]$cs)))  # TRUE
}
}
\author{
2026 Tobias Schmidt: initial version.
}
