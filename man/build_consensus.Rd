% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align.R
\name{build_consensus}
\alias{build_consensus}
\title{Build a consensus peak-list reference for NW snapping}
\usage{
build_consensus(x, y = NULL, gap_tol = 0.02)
}
\arguments{
\item{x}{A \code{decons2} object (training deconvolutions).}

\item{y}{Optional factor of class labels (length == \code{length(x)}). If
supplied, the seed consensus is built per-class.}

\item{gap_tol}{Numeric scalar. Tolerance in ppm for both the per-spectrum
NW snap and for deduplicating consensus positions. Default 0.02.}
}
\value{
A list with components \code{cssh}, \code{lcpar} (data frame with \code{x0},
\code{A}, \code{lambda}, \code{pcide}), plus the class attributes
\code{c("consensus", "align", "decon2", "spectrum")} so it behaves like a
single-spectrum reference for downstream code.
}
\description{
Constructs a single consensus peak list to be used as the alignment
target by \code{\link[=snap_nw]{snap_nw()}}. The consensus represents the union
of training-set peak positions, deduplicated within \code{gap_tol} ppm so
near-coincident peaks collapse to a single column.

When \code{y} is supplied, the consensus is class-aware: one
\code{\link[=find_ref]{find_ref()}} is picked per level of \code{y}, the per-class
references are merged into a seed consensus (so class-specific peaks
are present from the start), and every training spectrum is then
NW-snapped to this seed; their \code{x0} values contribute additional
consensus positions wherever they did not match the seed. The final
consensus is again deduplicated within \code{gap_tol}.

The returned object is shaped like an aligned spectrum
(\code{list(cssh, lcpar, ...)}) so it can be passed directly as \code{ref} to
\code{\link[=snap_nw]{snap_nw()}} at prediction time.
}
\author{
2026 Tobias Schmidt: initial version.
}
