% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mdm.R
\name{mdm_methods}
\alias{mdm_methods}
\alias{predict.mdm}
\alias{print.mdm}
\alias{coef.mdm}
\alias{plot.mdm}
\alias{summary.mdm}
\alias{print.summary.mdm}
\title{S3 methods for mdm objects}
\usage{
\method{predict}{mdm}(
  object,
  newdata,
  type = c("all", "prob", "class", "link"),
  s = "lambda.min",
  nworkers = 1,
  verbosity = 1,
  ...
)

\method{print}{mdm}(x, ...)

\method{coef}{mdm}(object, ...)

\method{plot}{mdm}(x, ...)

\method{summary}{mdm}(object, ...)

\method{print}{summary.mdm}(x, ...)
}
\arguments{
\item{object, x}{A fitted \code{mdm} object (for \code{predict}, \code{coef}, \code{summary}, \code{print} and \code{plot})
or a \code{summary.mdm} object (for \code{print.summary.mdm}).}

\item{newdata}{Spectra object or numeric feature matrix.}

\item{type}{Prediction type, one of \code{"all"}, \code{"prob"}, \code{"class"}, \code{"link"}.}

\item{s}{Regularization value for lasso predictions.}

\item{nworkers}{Number of workers to deconvolute and align \code{newdata}.}

\item{verbosity}{Integer verbosity level.}

\item{...}{Passed to underlying methods where applicable.}
}
\value{
\itemize{
\item \code{predict}: numeric vector of probabilities, classes, and/or link scores.
\item \code{print}: invisibly returns \code{x}.
\item \code{coef}: coefficient object from \code{glmnet}.
\item \code{plot}: invisibly returns \code{NULL}.
\item \code{summary}: object of class \code{summary.mdm}.
\item \code{print.summary.mdm}: invisibly returns \code{x}.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\strong{WARNING: These methods are experimental and must not be used in
production. Their API is very likely to change in non-backwards-compatible
ways over the next few weeks.}

S3 methods for objects of class \code{mdm} and \code{summary.mdm}.

\code{predict.mdm()} predicts probabilities, classes, link scores, or all
three from an \code{mdm} object. When \code{newdata} is a spectra object, the
spectra are deconvoluted, aligned and snapped to the reference stored in
the model before prediction. When \code{newdata} is a numeric matrix, it is
used directly as the feature matrix.

\code{print.mdm()} prints a compact model summary.

\code{coef.mdm()} returns lasso coefficients.

\code{plot.mdm()} plots the lasso path.

\code{summary.mdm()} builds a compact summary list.

\code{print.summary.mdm()} prints formatted output for \code{summary.mdm} objects.
}
\examples{
m <- structure(
  list(
    model = NULL,
    ref = NULL,
    meta = list(npmax = 1000, nfit = 3, smit = 2, smws = 5,
                delta = 6.4, maxShift = 100, maxCombine = 0,
                maxSnap = 20)
  ),
  class = "mdm"
)
print(m)
summary(m)

\dontrun{
  m <- fit_mdm_grid(spectra, y, sfr = c(11, -2))
  predict(m, test_spectra, type = "prob")
  coef(m)
  plot(m)
}
}
