% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class.R
\name{is_spectrum}
\alias{is_spectrum}
\alias{is_spectra}
\title{Is an Object a spectrum or spectra?}
\usage{
is_spectrum(x)

is_spectra(x)
}
\arguments{
\item{x}{The object to check.}
}
\value{
\code{TRUE} if the object inherits from the named class, else \code{FALSE}.
}
\description{
Check if an object inherits from one of the base metabodecon classes
(\code{spectrum} or \code{spectra}). Since deconvoluted (\code{decon2}) and aligned
(\code{align}) objects inherit from \code{spectrum} (and \code{decons2}/\code{aligns} from
\code{spectra}), they also satisfy these checks. To test for a specific
lifecycle stage, use \code{\link[base:class]{base::inherits()}} directly, e.g.
\code{inherits(x, "decon2")} or \code{inherits(x, "aligns")}. See
\link{metabodecon-classes}.
}
\examples{
is_spectrum(sim[[1]])  # TRUE
is_spectra(sim[1:2])   # TRUE

d <- deconvolute(sim[[1]], sfr = c(3.55, 3.35))
is_spectrum(d)              # TRUE (decon2 inherits from spectrum)
inherits(d, "decon2")       # TRUE
}
\author{
2024-2025 Tobias Schmidt: initial version.
}
